<!-- retry.ejs (Retry payment page) -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; padding: 50px; }
        .container { max-width: 400px; margin: auto; padding: 20px; border: 1px solid #f44336; border-radius: 8px; background-color: #ffebee; }
        .error { color: #f44336; }
        button { background-color: #d32f2f; color: white; border: none; padding: 10px 20px; font-size: 16px; cursor: pointer; border-radius: 5px; }
        button:hover { background-color: #b71c1c; }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="error">Payment Failed!</h1>
        <p>Something went wrong. Please try again.</p>
        <a href="/shop"><button>Retry Payment</button></a>
    </div>
</body>
</html>
